# Runtime deps - kept in sync with pyproject.toml (the authoritative metadata).
pydicom>=3.0.1
fhir.resources>=8.0.0
html5lib>=1.1
pandas>=2.3.0
PyYAML>=6.0.2
python-dateutil
pydantic>=2.11.5
# Extras used only by the offline terminology scraper (build_terminologies.py).
beautifulsoup4>=4.13.4
lxml
